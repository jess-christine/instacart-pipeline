Instructions for data_dictionary.md:

1) Document each table with: table name, grain, primary key, foreign keys, column list (name, type, description), and sample values.
2) Add derived measures and any calculation logic used in analytics (e.g., repeat purchase rate formula).
3) Keep the dictionary versioned; update on each schema evolution change and reference audit.version_tag.
4) Provide a search-friendly format (YAML or markdown tables) to aid automation.
