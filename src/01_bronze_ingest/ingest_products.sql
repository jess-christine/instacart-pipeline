Purpose: Ingest products master files into bronze.

Instructions - what to put here:
1) Document expected product file schema and encoding (CSV/JSON, columns order, nullable fields).
2) Provide COPY/LOAD command examples for target platform and any file_format definitions required.
3) Include validation guidance for product_id uniqueness, nulls, and allowed values.
4) Describe post-ingest steps: record source_file in processed_files, capture row counts, and produce a sample SELECT for verification.
