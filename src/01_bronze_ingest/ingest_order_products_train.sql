Purpose: Ingest order_products__train dataset into bronze for training data.

Instructions - what to put here:
1) Mirror the prior dataset ingestion guidance but document naming conventions that distinguish train vs prior in processed_files.
2) Provide examples for testing that both datasets can be unioned later without schema conflicts.
3) Add notes on handling duplicates between train and prior if encountered.
